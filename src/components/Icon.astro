---
interface Props {
  name: string;
  size?: number;
  class?: string;
}

const { name, size = 24, class: className = '' } = Astro.props;
const iconPath = `/icons/${name.toLowerCase()}.svg`;
---

<img
  src={iconPath}
  alt={name}
  width={size}
  height={size}
  class={className}
/>
