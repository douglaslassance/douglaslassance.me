---
interface Props {
  title: string;
  description: string;
  url: string;
  image: string;
  type?: 'youtube' | 'instagram' | 'website' | 'github';
}

const { title, description, url, image, type } = Astro.props;
---

<a 
  href={url}
  target="_blank"
  rel="noopener noreferrer"
  class="card bg-base-100 shadow-sm hover:shadow-xl transition-all duration-300 h-full p-6"
>
  <div class="flex flex-row gap-6 h-full">
    <div class="flex-1 flex flex-col justify-between">
      <div>
        <h3 class="card-title text-base font-semibold mb-2 text-base-content/80">{title}</h3>
        <p class="text-sm text-base-content/60">{description}</p>
      </div>
    </div>
    
    <div class="w-48 flex-shrink-0 rounded-lg overflow-hidden bg-base-200">
      <img
        src={image}
        alt={title}
        class="w-full h-full object-cover aspect-video"
      />
    </div>
  </div>
</a>
